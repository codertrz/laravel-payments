language: php

services:
  - mysql

php:
  - 5.6

before_script:
  - cp .env.travis .env
  - mysql -e 'CREATE DATABASE IF NOT EXISTS test;'
  - composer self-update
  - composer install --no-interaction

script:
  - composer test
